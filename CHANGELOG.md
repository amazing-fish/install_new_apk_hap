# 变更记录

## Unreleased - bugfix
- 安装前刷新使用点击安装瞬间的设备选择快照，仍在线的多设备会恢复界面选中状态。
- “刷新设备”和“扫描最新包”按钮改为联动刷新设备与安装包，并同步按钮禁用状态。
- APK/HAP 下拉列表展示目录内全部候选，固定显示最多 10 行并支持滚动查看更多安装包。
- `-t` 设置按钮改为保存当前勾选状态：勾选时记忆，未勾选时删除对应 APK 的记忆。
- 自定义名称输入区改为自适应布局，避免固定宽度挤压操作按钮。

## v0.4.9 - ci
- 更新版本号到 `v0.4.9`，用于实测版本化 Windows EXE 构建流程。
- 验证手动触发构建时，workflow 会校验输入版本与 `VERSION` 文件一致，并生成带版本号的构建产物。

## v0.4.8 - refactor
- 文档治理：将 `v0.1.0~v0.1.13` 汇总为 `v0.1.x`，`v0.2.0~v0.2.3` 汇总为 `v0.2.x`，`v0.3.0~v0.3.3` 汇总为 `v0.3.x`，保留 `v0.4.x` 全量明细，降低历史噪音。
- 基于当前代码完成技术路径核对，并同步更新 Anchor 文档中的“代码分析基线”，确保实现与修改日志不漂移。

## v0.4.7 - bugfix
- 修复安装参数快照时机问题：将 `selected_apk`、`selected_hap`、`allow_test` 的快照前移到点击安装瞬间，并跨安装前刷新链路传递。
- 避免预刷新（`detect_devices`）期间用户切换包选择导致本次安装任务使用到后续变更的包路径或参数。

## v0.4.6 - bugfix
- 修复安装线程读取可变包选择导致的一致性问题：安装启动后若切换 APK/HAP 下拉选择，不再影响当前安装任务使用的包路径与 `-t` 参数。
- 安装线程改为使用主线程快照的 `selected_apk`、`selected_hap` 与 `allow_test`，避免出现“新包 + 旧参数”组合。

## v0.4.5 - bugfix
- 修复安装线程异常状态语义：`adb/hdc` 启动失败、安装包路径丢失等运行时异常不再显示为“已中止”，改为“安装异常”。
- 区分“用户主动中止”与“安装执行异常”两类路径，仅用户中止时进入“正在中止 -> 已中止”状态流转。

## v0.4.4 - bugfix
- 修复安装线程在后台直接读取 Tk 变量导致的线程安全问题：设备安装过程中断连/重连后，点击“中止下载”可能卡在“正在中止”。
- 安装参数改为在主线程快照后传入安装线程，并为安装线程增加异常兜底，确保状态可回收为“已中止/安装完成”。

## v0.4.3 - bugfix
- 修复 PR #24 覆盖 Android 崩溃日志入口的问题：“获取崩溃日志”会根据选中设备平台自动分发，Android 走 `adb dumpsys dropbox`，Harmony 走 `hdc faultlogger` 打包。

## v0.4.2 - feature
- 刷新设备列表时识别相对上次新增的设备，将新增设备置顶并以浅绿色高亮，同时在日志区记录新增设备码。

## v0.4.1 - feature
- 设备列表“获取崩溃日志”改为 Harmony 场景：拉取 `/data/log/faultlog/faultlogger` 后筛选最近 7 天 crash 文件并打包 zip。
- 新增“获取NEXTdemo日志”按钮：匹配相对路径 `haps/entry/files/log-ads`，批量拉取并打包 zip。
- 日志打包路径统一：Windows 输出到 `D:\`，非 Windows 输出到 `~/install_new_apk_hap_logs`。

## v0.4.0 - feature
- 设备列表新增“获取崩溃日志”按钮，支持对 Android 设备执行 `adb -s <device_id> shell dumpsys dropbox --print`。
- 崩溃日志获取成功后会将输出内容追加写入 `D:\crash.log`，并在日志区记录执行命令与结果。

## v0.3.x - feature/bugfix（汇总）
- 新增 NEXT（Harmony）设备 UDID 获取能力：支持调用 `hdc -t <device_id> shell bm get --udid`，成功后弹窗展示并复制到剪贴板；Android 设备提示不支持。
- 将 UDID 获取改为后台线程执行并增加按钮禁用态，避免慢响应阻塞 UI。
- 增加 UDID 命令返回码与输出有效性校验，避免错误文本被当作 UDID 展示。

## v0.2.x - feature/bugfix（汇总）
- APK/HAP 默认选中最新安装包，下拉展示最近 5 个候选。
- 安装中按钮切换为“中止下载”，支持中止安装流程并展示状态。
- 安装执行轮询增加等待，降低空转导致的 CPU 占用。
- Android 设备探测过滤 `emulator-xxx`，避免虚拟设备污染真实设备列表。

## v0.1.x - feature/bugfix（汇总）
- 完成首版 APK/HAP 安装 GUI：设备检测、最新包扫描、安装执行、设备自定义命名。
- 设备列表支持设备码/名称/状态/平台展示，并在单设备场景自动选中；列表高度在 8 行内自适应。
- 关键耗时操作（设备刷新、安装）后台线程化，避免 UI 主线程卡死。
- 配置文件落盘到 AppData（Windows）并补齐本地配置自动生成/更新说明。
- 支持日志复制与清空；补充调试日志输出。
- 增加 Windows exe 自动化构建与 release 资产上传流程。
