# 外显重构跟踪

## 当前进展（2026-06-04）
- 阶段一已完成：设备表格列顺序、设备统计摘要、已选设备摘要和安装包摘要均已接入。
- 阶段二已完成：UI 显示格式化、ttk 样式配置、界面布局构建已从 `src/main.py` 拆到独立模块。
- 阶段三已完成：主界面按“设备 -> 安装包 -> 执行 -> 日志”重排，平台限定动作根据当前选择启停，安装中按钮切换中止语义和危险态样式。
- 收口修复已完成：窗口收窄后主内容挂载到整页纵向滚动容器，安装包、日志、设备列表保留各自必要的局部滚动。

## 阶段一：状态接入与显示一致性
目标：修复外显改造中已经引入但未完整接入的状态展示问题，确保界面看到的内容与内部选择一致。

验收标准：
- [x] 设备表格列顺序正确：名称、平台、状态、设备码。
- [x] 设备刷新后，设备统计摘要同步更新。
- [x] 设备选择变化后，执行区与状态栏的已选设备摘要同步更新。
- [x] APK/HAP 扫描和下拉切换后，安装包摘要同步更新。
- [x] 关键格式化逻辑有单元测试覆盖。

## 阶段二：UI 结构拆分
目标：把 `src/main.py` 中的视觉样式、布局构建和业务编排拆出边界，降低后续外观迭代成本。

验收标准：
- [x] 样式常量和 ttk style 配置从 `App` 主类中拆出。
- [x] 设备区、安装包区、执行区、日志区形成清晰的构建边界。
- [x] `App` 保留应用状态编排和后台线程回写，不再承载大段重复布局代码。
- [x] 拆分后现有测试全部通过，交互行为不漂移。

## 阶段三：体验重排与视觉完善
目标：在结构稳定后做真正的外显升级，而不是继续在单文件里叠样式。

验收标准：
- [x] 主工作流按“设备 -> 安装包 -> 执行 -> 日志”清晰组织。
- [x] 高风险动作、耗时动作、只支持特定平台的动作有明确状态反馈。
- [x] 窗口在最小尺寸和常用桌面尺寸下内容不重叠、不截断。
- [x] 主界面内容超过视口时支持整体纵向滚动。
- [x] 日志、状态栏和弹窗文案语义一致。

## 验证
- `python -m pytest -q`
- `python -m py_compile src/main.py src/ui_display.py src/ui_layout.py src/ui_styles.py`
- 人工启动窗口走查：默认尺寸 380x700，最小尺寸 380x600，最小高度下主界面可整体下滑。
